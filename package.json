{
  "name": "hole-simple-project",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "@types/lodash": "^4.14.184",
    "lerna": "^5.5.1",
    "lodash": "^4.17.21",
    "typescript": "^4.8.3"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  },
  "scripts": {
    "dev": "lerna run dev --stream --parallel",
    "test": "lerna run test --",
    "build": "lerna run build",
    "build:all": "tsc"
  },
  "workspaces": [
    "packages/*"
  ]
}
